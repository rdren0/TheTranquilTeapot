node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Purchased art assets — licensed to this project, NOT for redistribution.
# Keep them out of version control (especially if the repo is ever public).
